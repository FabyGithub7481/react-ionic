import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'curso-react-ionic-lm',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
